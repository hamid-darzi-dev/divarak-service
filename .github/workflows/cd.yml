name: CD - Deploy
on:
    push:
        branches: [ "main" ]



jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up JDK 21
              uses: actions/setup-java@v4
              with:
                  distribution: temurin
                  java-version: 21

            - name: Cache Gradle dependencies
              uses: actions/cache@v4
              with:
                  path: |
                      ~/.gradle/caches
                      ~/.gradle/wrapper
                  key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', 'gradle/libs.versions.toml') }}
                  restore-keys: |
                      ${{ runner.os }}-gradle-

            - name: Build JAR
              run: ./gradlew bootJar

            - name: Configure AWS credentials
              uses: aws-actions/configure-aws-credentials@v4
              with:
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                  aws-region: eu-central-1

            - name: Login to Amazon ECR
              id: login-ecr
              uses: aws-actions/amazon-ecr-login@v2

            - name: Set short git commit SHA
              run: |
                  calculatedSha=$(git rev-parse --short ${{ github.sha }})
                  echo "COMMIT_SHORT_SHA=$calculatedSha" >> $GITHUB_ENV

            - name: Confirm git commit SHA output
              run: |
                  echo "This is short commit sha:${{ env.COMMIT_SHORT_SHA }}"

            - name: Build and tag Docker image
              run: |
                  docker build -t divarak-service:${{ env.COMMIT_SHORT_SHA }} .
                  docker tag divarak-service:${{ env.COMMIT_SHORT_SHA }} 596484148546.dkr.ecr.eu-central-1.amazonaws.com/divarak/divarak-service:${{ env.COMMIT_SHORT_SHA }}

            - name: Push docker image to ECR
              run: docker push 596484148546.dkr.ecr.eu-central-1.amazonaws.com/divarak/divarak-service:${{ env.COMMIT_SHORT_SHA }}

            - name: Deploy to EC2
              uses: appleboy/ssh-action@v1.2.0
              with:
                  key: ${{ secrets.EC2_SSH_KEY }}
                  username: ubuntu
                  host: ${{ secrets.EC2_HOST }}
                  script: |
                      aws ecr get-login-password --region eu-central-1 \
                        | docker login --username AWS --password-stdin 596484148546.dkr.ecr.eu-central-1.amazonaws.com

                      docker pull 596484148546.dkr.ecr.eu-central-1.amazonaws.com/divarak/divarak-service:${{ env.COMMIT_SHORT_SHA }}

                      docker rm -f divarak-service || true

                      docker images 596484148546.dkr.ecr.eu-central-1.amazonaws.com/divarak --format "{{.ID}}" \
                        | xargs -r docker rmi -f

                      docker run -d --name divarak-service \
                        -p 8080:8080 \
                        -e "SPRING_DATASOURCE_URL=jdbc:postgresql://${{ secrets.RDS_HOST }}:5432/divarak_service?currentSchema=public" \
                        -e "SPRING_DATASOURCE_USERNAME=${{ secrets.RDS_USER }}" \
                        -e "SPRING_DATASOURCE_PASSWORD=${{ secrets.RDS_PASSWORD }}" \
                        596484148546.dkr.ecr.eu-central-1.amazonaws.com/divarak/divarak-service:${{ env.COMMIT_SHORT_SHA }}
